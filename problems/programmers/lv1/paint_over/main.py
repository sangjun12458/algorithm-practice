def solution(n, m, section):
    answer = 0

    painted = [0] * (n+1)
    for s in section:
        painted[s] = -1

    # preprocess

    while True:
        # paint
        break

    return answer